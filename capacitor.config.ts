import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.edu.pecs',
  appName: 'PECS',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
